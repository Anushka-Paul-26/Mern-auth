# my-auth-app
A React-based authentication system with a clean and responsive login/signup page, integrated with React Router for client-side routing and context API for global state management. This project demonstrates a refresh-safe routing setup, secure login flow, and a home page redirect after successful authentication.
